# UPF (Universal Presentation Framework)
UPF是一个通用跨平台的UI呈现框架，框架目的是为了让WPF(Windows Presentation Framework)程序移植到多个OS平台。
UPF的API可以说是基于WPF，参考UWP、WinUI，并在这之上进行改进优化。

目前UPF还处于基础框架开发过程。
`UniversalPresentationFramework.Test.Win32``项目可以直接启动。